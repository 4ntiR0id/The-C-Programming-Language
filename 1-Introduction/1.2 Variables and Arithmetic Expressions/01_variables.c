/*
 * ============================================================================
 * Chapter 1: Introduction
 * Section 1.2: Variables and Arithmetic Expressions
 * Topic: Variables
 * ============================================================================
 * 
 * ما هي المتغيرات (Variables)؟
 * -----------------------------
 * المتغير هو مكان في الذاكرة نستخدمه لتخزين قيمة.
 * يمكننا تغيير هذه القيمة أثناء تشغيل البرنامج.
 * 
 * لماذا نحتاج المتغيرات؟
 * ------------------------
 * - لتخزين البيانات التي نعمل عليها
 * - للاحتفاظ بنتائج العمليات الحسابية
 * - لجعل البرامج قادرة على معالجة بيانات مختلفة
 * 
 * أنواع المتغيرات الأساسية في C:
 * ---------------------------------
 * int    - للأعداد الصحيحة (integers): 1, 2, -5, 100
 * float  - للأعداد العشرية (floating point): 3.14, -0.5, 2.0
 * char   - للحروف (characters): 'a', 'Z', '5'
 * 
 * ============================================================================
 */

main()
{
    /*
     * إعلان المتغيرات (Variable Declaration)
     * --------------------------------------
     * قبل استخدام أي متغير، يجب أن نُعلن عنه أولاً
     * الإعلان يخبر المترجم:
     * 1. اسم المتغير
     * 2. نوع البيانات التي سيخزنها
     * 
     * الصيغة: type variable_name;
     */
    
    /*
     * هنا نُعلن عن متغير اسمه celsius من نوع int
     * int تعني أن المتغير سيخزن عدد صحيح
     * لماذا int؟ لأننا سنخزن درجات حرارة صحيحة مثل 0, 20, 100
     */
    int celsius;
    
    /*
     * إعلان عن متغير آخر اسمه fahr (اختصار لـ Fahrenheit)
     * أيضاً من نوع int
     */
    int fahr;
    
    /*
     * يمكننا إعلان عدة متغيرات من نفس النوع في سطر واحد
     * بوضع فاصلة بينها:
     */
    int lower, upper, step;
    
    
    /*
     * إسناد القيم (Assignment)
     * -------------------------
     * بعد الإعلان، نستطيع إعطاء المتغير قيمة
     * نستخدم علامة = للإسناد
     * 
     * مهم: علامة = في C تعني "أسند" وليس "يساوي"
     */
    
    /*
     * نُسند القيمة 0 للمتغير lower
     * lower سيخزن الآن القيمة 0
     */
    lower = 0;
    
    /*
     * نُسند 300 للمتغير upper
     */
    upper = 300;
    
    /*
     * نُسند 20 للمتغير step
     */
    step = 20;
    
    /*
     * نُسند قيمة lower (وهي 0) للمتغير fahr
     * الآن fahr يساوي 0
     */
    fahr = lower;
    
    
    /*
     * استخدام المتغيرات
     * ------------------
     * بعد الإعلان والإسناد، نستطيع استخدام المتغيرات
     */
    
    /*
     * نطبع قيمة المتغير fahr
     * %d تعني "ضع هنا قيمة عدد صحيح"
     * fahr هو المتغير الذي سيُطبع مكان %d
     */
    printf("Temperature: %d\n", fahr);
    
    /*
     * يمكننا طباعة عدة متغيرات:
     * %d الأولى ستُستبدل بقيمة lower
     * %d الثانية ستُستبدل بقيمة upper
     */
    printf("Range: %d to %d\n", lower, upper);
}

/*
 * ============================================================================
 * قواعد تسمية المتغيرات:
 * ============================================================================
 * 
 * مسموح:
 * -------
 * - الحروف (a-z, A-Z)
 * - الأرقام (0-9) لكن ليس كأول حرف
 * - الشرطة السفلية (_)
 * 
 * أمثلة صحيحة:
 * temperature, temp1, _value, myVariable
 * 
 * 
 * غير مسموح:
 * -----------
 * - البدء برقم: 1temp (خطأ)
 * - استخدام مسافات: my temp (خطأ)
 * - استخدام رموز خاصة: temp@, value$ (خطأ)
 * - الكلمات المحجوزة: int, float, while (خطأ)
 * 
 * 
 * نصائح:
 * -------
 * - استخدم أسماء واضحة تصف الغرض: celsius أفضل من c
 * - C حساسة لحالة الأحرف: Temp و temp مختلفان
 * - اتبع أسلوب واحد: إما lower_case أو camelCase
 * 
 * 
 * ============================================================================
 * Expected Output:
 * ============================================================================
 * Temperature: 0
 * Range: 0 to 300
 * 
 * 
 * ============================================================================
 * ملاحظات مهمة:
 * ============================================================================
 * 
 * 1. الإعلان يجب أن يكون قبل الاستخدام
 *    - لا يمكنك استخدام متغير قبل إعلانه
 * 
 * 2. المتغيرات غير المُهيأة
 *    - إذا أعلنت عن متغير ولم تُسند له قيمة، قيمته غير محددة
 *    - قد تحتوي على أي قيمة عشوائية
 *    - دائماً أسند قيمة أولية قبل الاستخدام
 * 
 * 3. في النسخة الأولى من K&R
 *    - كل الإعلانات تكون في بداية الدالة
 *    - لا يمكن الإعلان في منتصف الكود
 * 
 * 4. %d في printf
 *    - تُستخدم لطباعة أعداد صحيحة (int)
 *    - عدد علامات %d يجب أن يساوي عدد المتغيرات
 * 
 * ============================================================================
 */

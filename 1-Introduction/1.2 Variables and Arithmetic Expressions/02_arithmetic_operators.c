/*
 * ============================================================================
 * Chapter 1: Introduction
 * Section 1.2: Variables and Arithmetic Expressions
 * Topic: Arithmetic operators
 * ============================================================================
 * 
 * ما هي المعاملات الحسابية؟
 * ----------------------------
 * هي الرموز التي نستخدمها لإجراء العمليات الحسابية
 * مثل الجمع والطرح والضرب والقسمة
 * 
 * لماذا مهمة؟
 * -----------
 * - أساس أي حسابات في البرنامج
 * - تسمح بمعالجة البيانات رياضياً
 * - ضرورية لبناء برامج مفيدة
 * 
 * ============================================================================
 */

main()
{
    /*
     * إعلان المتغيرات التي سنستخدمها
     */
    int a, b, result;
    int fahr, celsius;
    
    /*
     * ============================================================================
     * المعاملات الحسابية الأساسية في C
     * ============================================================================
     */
    
    /*
     * 1. الجمع: +
     * -----------
     */
    a = 10;
    b = 5;
    result = a + b;          /* result الآن يساوي 15 */
    printf("10 + 5 = %d\n", result);
    
    
    /*
     * 2. الطرح: -
     * -----------
     */
    result = a - b;          /* result الآن يساوي 5 */
    printf("10 - 5 = %d\n", result);
    
    
    /*
     * 3. الضرب: *
     * -----------
     * ملاحظة: نستخدم * وليس × للضرب
     */
    result = a * b;          /* result الآن يساوي 50 */
    printf("10 * 5 = %d\n", result);
    
    
    /*
     * 4. القسمة: /
     * ------------
     * مهم جداً: القسمة بين أعداد صحيحة تعطي عدد صحيح
     * الكسر العشري يُحذف (لا يتم تقريبه)
     */
    result = a / b;          /* result الآن يساوي 2 */
    printf("10 / 5 = %d\n", result);
    
    /*
     * مثال يوضح حذف الكسر العشري:
     */
    a = 10;
    b = 3;
    result = a / b;          /* result = 3 وليس 3.333 */
    printf("10 / 3 = %d (decimal part discarded)\n", result);
    
    
    /*
     * 5. باقي القسمة (Modulus): %
     * -----------------------------
     * يعطي باقي القسمة
     */
    a = 10;
    b = 3;
    result = a % b;          /* result = 1 (لأن 10÷3 = 3 والباقي 1) */
    printf("10 %% 3 = %d\n", result);
    
    
    /*
     * ============================================================================
     * مثال عملي: تحويل درجة الحرارة من Fahrenheit إلى Celsius
     * ============================================================================
     * 
     * المعادلة: celsius = 5 * (fahr - 32) / 9
     * 
     * لماذا هذا الترتيب مهم؟
     * - نطرح 32 أولاً
     * - نضرب في 5
     * - نقسم على 9 أخيراً
     * الأقواس تحدد أولوية العمليات
     */
    
    fahr = 100;
    
    /*
     * الطريقة الصحيحة:
     * نستخدم أقواس لتحديد الترتيب
     */
    celsius = 5 * (fahr - 32) / 9;
    
    printf("%d Fahrenheit = %d Celsius\n", fahr, celsius);
    
    
    /*
     * ============================================================================
     * أولوية العمليات (Operator Precedence)
     * ============================================================================
     * 
     * C تُنفذ العمليات بترتيب معين:
     * 
     * 1. الأقواس ()              - أعلى أولوية
     * 2. الضرب * والقسمة / و %   - أولوية متوسطة
     * 3. الجمع + والطرح -        - أولوية أقل
     * 
     * العمليات بنفس الأولوية تُنفذ من اليسار لليمين
     */
    
    /*
     * مثال على أهمية الأقواس:
     */
    a = 5;
    b = 3;
    
    result = a + b * 2;      /* الضرب أولاً: 3*2=6، ثم الجمع: 5+6=11 */
    printf("5 + 3 * 2 = %d\n", result);
    
    result = (a + b) * 2;    /* الأقواس أولاً: 5+3=8، ثم الضرب: 8*2=16 */
    printf("(5 + 3) * 2 = %d\n", result);
    
    
    /*
     * ============================================================================
     * تعبيرات معقدة (Complex Expressions)
     * ============================================================================
     */
    
    /*
     * يمكننا كتابة تعبيرات طويلة:
     */
    a = 10;
    b = 5;
    result = (a + b) * (a - b) / 2;
    /*
     * الترتيب:
     * 1. (a + b) = 15
     * 2. (a - b) = 5
     * 3. 15 * 5 = 75
     * 4. 75 / 2 = 37 (الكسر يُحذف)
     */
    printf("(10+5)*(10-5)/2 = %d\n", result);
}

/*
 * ============================================================================
 * Expected Output:
 * ============================================================================
 * 10 + 5 = 15
 * 10 - 5 = 5
 * 10 * 5 = 50
 * 10 / 5 = 2
 * 10 / 3 = 3 (decimal part discarded)
 * 10 % 3 = 1
 * 100 Fahrenheit = 37 Celsius
 * 5 + 3 * 2 = 11
 * (5 + 3) * 2 = 16
 * (10+5)*(10-5)/2 = 37
 * 
 * 
 * ============================================================================
 * ملاحظات مهمة:
 * ============================================================================
 * 
 * 1. القسمة الصحيحة (Integer Division)
 *    - 10 / 3 = 3 (وليس 3.333)
 *    - الكسر يُحذف تماماً
 *    - لا يوجد تقريب، فقط حذف
 * 
 * 2. باقي القسمة (%)
 *    - يعمل فقط مع الأعداد الصحيحة
 *    - مفيد لمعرفة إذا كان عدد زوجي (n % 2 == 0)
 * 
 * 3. الأقواس
 *    - استخدمها دائماً عندما تكون غير متأكد من الأولوية
 *    - تجعل الكود أوضح للقراءة
 * 
 * 4. المسافات
 *    - a+b و a + b متساويان
 *    - المسافات تُستخدم فقط لوضوح القراءة
 * 
 * 5. في printf
 *    - لطباعة % نكتبها مرتين: %%
 *    - لأن % لها معنى خاص في printf
 * 
 * ============================================================================
 */

/*
 * ============================================================================
 * Chapter 1: Introduction
 * Section 1.1: Getting Started
 * Topic: The first C program
 * ============================================================================
 * 
 * ما هو هذا البرنامج؟
 * ----------------
 * هذا هو أبسط برنامج في لغة C. الهدف منه هو طباعة نص على الشاشة.
 * يُعتبر نقطة البداية لفهم بنية أي برنامج C.
 * 
 * لماذا هذا مهم؟
 * --------------
 * - يعلمك البنية الأساسية لأي برنامج C
 * - يوضح كيفية استخدام الدوال (functions)
 * - يُظهر كيفية طباعة النصوص على الشاشة
 * - كل برنامج C يبدأ من الدالة main()
 * 
 * ============================================================================
 */

/* 
 * السطر التالي يُعرّف بداية الدالة الرئيسية main()
 * كل برنامج C يجب أن يحتوي على main() - هي نقطة البداية
 * الأقواس () تعني أن الدالة لا تستقبل أي مدخلات (parameters)
 */
main()
{
    /*
     * printf هي دالة جاهزة في C تستخدم لطباعة النصوص
     * الجملة بين علامتي التنصيص "" هي النص الذي سيُطبع
     * \n تعني "انتقل لسطر جديد" بعد طباعة النص
     * الفاصلة المنقوطة ; ضرورية في نهاية كل أمر (statement)
     * 
     * لماذا \n مهمة؟
     * - بدونها، سيظهر النص ملتصقاً بما يليه على نفس السطر
     * - تجعل الإخراج أكثر وضوحاً وتنظيماً
     */
    printf("hello, world\n");
}

/*
 * Expected Output:
 * ----------------
 * hello, world
 * 
 * ملاحظات مهمة:
 * -------------
 * 1. في النسخة الأولى من K&R، لا نكتب return 0 في نهاية main
 * 2. لا نستخدم #include <stdio.h> في الأمثلة الأولى
 * 3. الهدف هو البساطة المطلقة للمبتدئين
 * 4. هذا البرنامج يعمل في بيئة C التقليدية
 */

/*
 * compilation_execution.c
 * 
 * A program must be compiled before it can run.
 * The compiler translates C code into machine instructions.
 */

#include <stdio.h>

main()
{
    printf("This program was compiled and is now running\n");
    
    /* 
     * لتحويل هذا الملف إلى برنامج قابل للتشغيل:
     * cc compilation_execution.c
     * ثم تشغيله:
     * a.out
     */
}

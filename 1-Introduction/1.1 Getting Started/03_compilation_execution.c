/*
 * ============================================================================
 * Chapter 1: Introduction
 * Section 1.1: Getting Started
 * Topic: Compilation and execution
 * ============================================================================
 * 
 * ما نتعلمه هنا؟
 * ---------------
 * كيف نحوّل الكود المكتوب بلغة C إلى برنامج يعمل على الحاسوب.
 * 
 * لماذا مهم فهم هذه العملية؟
 * ---------------------------
 * - C هي لغة مُترجمة (compiled)، وليست لغة مُفسرة (interpreted)
 * - يجب تحويل الكود إلى لغة الآلة قبل التشغيل
 * - فهم العملية يساعدك على حل المشاكل
 * 
 * ============================================================================
 */

/*
 * هذا برنامج بسيط سنستخدمه كمثال على عملية الترجمة والتنفيذ
 */
main()
{
    printf("Understanding compilation\n");
}

/*
 * ============================================================================
 * خطوات تحويل البرنامج من كود إلى تنفيذ:
 * ============================================================================
 * 
 * الخطوة 1: كتابة الكود (Source Code)
 * -------------------------------------
 * - نكتب الكود في ملف نصي بامتداد .c
 * - مثلاً: hello.c
 * - هذا الملف يحتوي على كود بلغة C يمكن للإنسان قراءته
 * 
 * 
 * الخطوة 2: الترجمة (Compilation)
 * ---------------------------------
 * - نستخدم مترجم C (compiler) لتحويل الكود
 * - الأمر الكلاسيكي في UNIX:
 *   
 *   cc hello.c
 *   
 * - cc تعني "C Compiler"
 * - hello.c هو اسم ملف الكود المصدري
 * 
 * ماذا يحدث أثناء الترجمة؟
 * - المترجم يقرأ كل سطر من كودك
 * - يتحقق من عدم وجود أخطاء (syntax errors)
 * - يحول الكود إلى لغة الآلة (machine code)
 * - ينتج ملف قابل للتنفيذ (executable file)
 * 
 * 
 * الخطوة 3: الملف التنفيذي (Executable)
 * ---------------------------------------
 * - بعد الترجمة الناجحة، يُنتج ملف اسمه a.out
 * - a.out يعني "assembler output" (ناتج المُجمّع)
 * - هذا الملف يحتوي على أوامر بلغة الآلة
 * - الحاسوب يستطيع تنفيذه مباشرة
 * 
 * 
 * الخطوة 4: التنفيذ (Execution)
 * -------------------------------
 * - لتشغيل البرنامج نكتب اسم الملف التنفيذي:
 *   
 *   a.out
 *   
 * - النظام يُحمّل البرنامج في الذاكرة
 * - يبدأ التنفيذ من الدالة main()
 * - يُنفذ كل أمر بالترتيب
 * - عند انتهاء main()، ينتهي البرنامج
 * 
 * 
 * ============================================================================
 * مثال كامل على العملية:
 * ============================================================================
 * 
 * لنفترض أن لدينا ملف اسمه hello.c يحتوي على:
 * 
 *   main()
 *   {
 *       printf("hello, world\n");
 *   }
 * 
 * الخطوات:
 * 
 * 1. نترجم البرنامج:
 *    $ cc hello.c
 *    
 *    (إذا لم تظهر رسائل خطأ، الترجمة نجحت)
 * 
 * 2. نشغل البرنامج:
 *    $ a.out
 *    hello, world
 *    $
 * 
 * 
 * ============================================================================
 * الأخطاء الشائعة:
 * ============================================================================
 * 
 * خطأ في الترجمة (Compilation Error):
 * -----------------------------------
 * إذا نسيت الفاصلة المنقوطة مثلاً:
 * 
 *   main()
 *   {
 *       printf("hello")    ← خطأ: لا يوجد ;
 *   }
 * 
 * المترجم سيعرض رسالة خطأ ولن ينتج ملف a.out
 * يجب تصحيح الخطأ وإعادة الترجمة
 * 
 * 
 * خطأ في التنفيذ (Runtime Error):
 * --------------------------------
 * البرنامج يُترجم بنجاح، لكن يحدث خطأ أثناء التشغيل
 * مثل: محاولة القسمة على صفر
 * 
 * 
 * ============================================================================
 * ملاحظات مهمة:
 * ============================================================================
 * 
 * 1. الترجمة تحدث مرة واحدة فقط
 *    - بعد إنتاج a.out، يمكنك تشغيله عدة مرات
 *    - لا حاجة لإعادة الترجمة إلا إذا غيرت الكود
 * 
 * 2. لغة C مُترجمة، وليست مُفسرة
 *    - Python مثلاً لغة مُفسرة (تُنفذ سطراً بسطر)
 *    - C تُترجم كاملة أولاً، ثم تُنفذ
 *    - هذا يجعل برامج C أسرع في التنفيذ
 * 
 * 3. يمكن تسمية الملف التنفيذي
 *    - بدلاً من a.out، يمكن استخدام:
 *      cc -o hello hello.c
 *    - هذا ينتج ملف اسمه hello بدلاً من a.out
 * 
 * 4. في النسخة الأولى من K&R
 *    - الأمثلة تفترض بيئة UNIX
 *    - الأمر cc هو المترجم القياسي
 *    - العملية بسيطة ومباشرة
 * 
 * ============================================================================
 */

/*
 * Expected Output عند تشغيل هذا البرنامج:
 * ----------------------------------------
 * Understanding compilation
 */
